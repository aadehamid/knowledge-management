# YouTube

## Fine-tune PaliGemma for image to JSON use cases

### Video Metadata
- **Keywords:** video, sharing, camera phone, video phone, free, upload
- **Runtime:** PT77M43S

### Description
In this tutorial, I'll showcase how to fine-tune PaliGemma, a new open vision-language model by Google on a receipt image to JSON use case. The goal for the model is to learn to output a JSON containing all key fields from a receipt, such as the product items, their prices and quantities.

Do note that PaliGemma is just one of many vision-language models released recently.

The notebook can be found here: https://github.com/NielsRogge/Transfo...

### Transcript
Hi everyone. So, recently Google released its new open vision language model called Pali Gemma or Pali Gamma, depending on how you prefer to pronounce it. Um and it's actually a very nice vision language model that you can fine-tune for your specific use case. So, that's really um yeah, the goal of this model actually is to be fine-tuned on a specific tasks. And so, um that's actually what I would like to illustrate in today's video. So, in my GitHub repository called Transformers tutorials, uh you can find all kinds of notebooks for all the different models available in the Transformers library by Hugging Face. Um and so, recently I updated a I uploaded a notebook for this Pali Gamma model where I fine-tuned it uh for a downstream task, mainly for converting receipt images into uh JSON. So, for for instance, if you have this receipt image, uh you would like to uh make the model learn to output a JSON containing all the item names, their quantities for for example, as well as the price, and then also the total price. Um and so, yeah, that's why I created this notebook. Um so, um I'm going to go over it cell by cell. And we're going to run it uh as well. So, you can actually copy it for example to your Google Drive, and then you can use Google Colab uh to run the notebook. So, of course, if you want to fine-tune such a model, you will need a GPU. Uh for this, I personally used a Nvidia L4 GPU. So, it's already selected here by default. So, yeah, make sure that you are running this notebook on a GPU. Um so, yeah, of course, there are various GPUs available on Google Colab. For instance, the T4 is more of an older generation, which has 16 GB of RAM. The L4 has 24 GB of RAM, and the A100 has either 40 or 80 GB of RAM. For this notebook, I think yeah, 24 will definitely suffice. I haven't actually tried it yet on the T4, but I would recommend using at least 24 GB of GPU RAM. Um so, the architecture of Polygemma is actually fairly simple. So, what they did was they took a pre-trained image encoder, and they took a pre-trained large language model, and they basically fit them together with a projection layer in between. And in this way, yeah, we basically have a multi-modal model that can take in both an image and a text as input, and then produce text as an output. So, in this case, they used SigLIP for the image encoder. So, SigLIP is a new model developed by Google, which builds upon the CLIP model from OpenAI. So, SigLIP is trained in a contrastive way on image-text pairs, and so you can then, yeah, only use the image encoder because SigLIP itself actually already consists of an image encoder and a text encoder. Um but so, we're only going to use the image encoder. And then, for the large language model, Gemma is a large language model that was released by Google some months ago. Um and so, by basically stitching together these two models, along with this projection layer that is then learned during training on image and text data, you get Poly Gemma. So, I'm going to start here by running the first few cells, which which set up our environment. So, here I'm installing the Transformers library from source. So, by source I mean that I'm installing here from GitHub right away. So, it will probably It'll basically install the main branch here that you can find on GitHub. But of course, you could also uh cuz actually there was a recent release which fixed fine-tuning of this new model. So, you need to have at least version 4.41.1 available for it to work. And then for loading a dataset, we're going to use the datasets library which is also developed by Hugging Face. For training, I'm going to be using PyTorch Lightning. Of course, there are many options here. You could also just train using native PyTorch or using frameworks like Accelerate or the Hugging Face Trainer API. I actually just updated the documentation of this PaliGemma model. So, I actually now link and if you go to the multi-modal model section and you search for PaliGemma, you will find a resources section. So, every model page has now also this a resources section where you find a link to some useful resources like demo notebooks, blog posts, and so on. And so, yeah, there are actually also notebooks available that leverage the Hugging Face Trainer API. So, if you're interested in that, feel free to check them out. The reason I went for PyTorch Lightning in my case is because I want to easily compute metrics on my evaluation set during training. And this is pretty straightforward for now to do with PyTorch Lightning. In the future, we might extend the Trainer API as well to support this more easily for multi-modal models. Another thing we will be installing here is the PEFT library from Hugging Face. So, uh, PEFT is a new library developed by Hugging Face for parameter efficient fine-tuning. Um, I'm going to be explaining uh, later on in the notebook what it actually means, but it basically will allow us to fine-tune the PaliGemma or any other large model on consumer hardware by only training a couple of adapter layers on top of the pre-trained model. Um, and for the quantization, which I also will explain in a bit, we need to install the accelerate library developed by Hugging Face and bitsandbytes. And finally, I'll also will be installing Weights & Biases uh, for logging. So, basically for seeing some metrics during training like seeing the loss going down or seeing the accuracy going up for for instance. All right, so we have installed all our necessary uh, libraries. Then, as the next step, I'm going to be defining some variables that I'm going to be using throughout this notebook. So, the repo ID is just uh, pre-trained model which we are going to fine-tune uh, from. So, actually, they released like a huge amount of checkpoints on the hub. So, there are already 143 PaliGemma models available and I think if you would search for the Google ones, there are 118 18 uh, released by Google. So, you can find them in all kinds of different image resolutions. Uh, up to 896 pixels. So, this is actually very useful for OCR type of tasks where the model really needs to see the details in the image of a PDF for instance. Um, and they released them in both the original JAX versions, which is developed which is then compatible with their original Big Vision uh, repository in which they actually released uh, PaliGemma. So, this entire library is developed in JAX, which is an alternative to PyTorch. Um but yeah, we will be using um the PyTorch version. Um so, for instance, this one This is actually the one we will be using in our notebook. Has 3 billion parameters. Uh so, this comes from the fact that Gemma, the large language model component, has 2 billion parameters, and then the vision encoder has approximately 1 billion trainable parameters along with the projection layer in between. So, that's why we have 3 billion in here. You can always also check the size of the model here by going to the files and versions tab. So, in this case, we have about 12 GB in size. Um so, that's actually explained by the fact that we have uh precision here of 32 bits or 4 bytes per parameter. So, 4 * 3 billion is 12 billion uh bytes, uh which comes down to uh 12 GB. Um and then yeah, this is the name of the fine-tuned model. So, I'm just going to be pushing the model during fine-tuning to the hub as well under my own personal username. Feel free to change this. Um the maximum length is the amount of tokens that we want the model to learn to generate during training. Of course, this is going to impact the amount of memory that you use. If I would use a smaller value here, uh yeah, we would require less memory. Uh but I used 512 tokens here for a good trade-off for our particular data set. And then finally, I set some variables here for the weights and biases project uh to which I will be logging my metrics. So, I'm going to be running this cell. And next, we can load uh our data set from the hub. So, a data set I often use for creating these demo notebooks is this CORD dataset created by the Donut authors. So, Donut is another document AI model available in the Transformers library. Um and so they created this nice uh data set here where you have the received images as an input um with the corresponding ground truth JSON as an output. So, this is actually what you need if you want to train such a vision language model. So, it's also not uh that large. It's like 1,000 rows, so it's ideal actually for demo purposes. Um so, it's just two lines of code to load this from the hub. Of course, um yeah, now so, Google Colab has this integration with the Hugging Face token, so that's really convenient. So, this also allows you to, for instance, automatically push private models to the Hugging Face hub with your token. Um or loads a private data set from the hub. Um So, this is called Google Colab secrets. Uh it's pretty handy if you want to uh yeah, link your Hugging Face token with the Google Colab environment. Uh but of course, if you want to create your own custom data set, I recommend to check out this guide over here in the documentation of Hugging Face datasets, which explains how you can create such a data set yourself. Um so, particularly these guides here uh are quite relevant. So, basically, you need to use this image folder uh feature here, and then you can specify a path to all your images, and then inside that same uh folder, you need to have this metadata.jsonl file, which can yeah, have any kind of metadata present for every single image. So, in this case, uh it might be this ground truth parsing uh format. So, we can then yeah, print out the datasets. So, what we get is a dataset dictionary. So, this is much like a regular Python dictionary containing various keys. So, we have a key for every single split here. So, one for training, 800 rows, one for validation, 100 rows, and one for testing. Uh so, not that big of a data set. Um, and then you can actually very easily visualize an example. Um, because if you look here at the features for instance of the training splits, uh, you can then actually also see what the type is of these individual columns. So, the image column is of type image, which means that you can like automatically visualize it in the browser. Here I'm just making it a bit smaller using Pillow. Um, because otherwise it would be quite large. And then the ground truth, uh, feature is just a string. Uh, but you can actually read it as JSON as follows. And then this is actually the ground truth JSON that, uh, authors of this data set created. Um, so yeah, you can see that it nicely corresponds to the items present on the receipt. And so this is actually exactly what we want the model to learn to generate given an image. But So, note that these models are very flexible. You can make them learn to generate whatever you want given an image and an optional text prompt. Um, so here I'm going from a receipt image to a JSON, but it could of course also go from, let's say, an image of a PDF to markdown, for instance, or to text right away. Uh, this yeah, depends a bit on what you want. Uh, and actually the authors of PolyJema are quite, um, creative because they actually trained it for several capabilities like image captioning. So, here you go from an image to a description of that image. Uh, you can do visual question answering where you give it an image together with a certain question, uh, and then the model generates an answer. Uh, it can even do object detection, uh, where you can prompt the model with the text prompt, for example, detect cat, and the model will basically output a sequence of tokens uh, representing the bounding box coordinates along with a class label. So, that's that's really nice, of course. This is yeah, using some special tokens in the vocabulary of the large language model. Um and then another task is called referring expression segmentation. So, in this case uh yeah, it again creates these masks. So, instead of bounding boxes, in this case it creates uh masks for these birds, for instance. Uh which is pretty nice. And then, yeah, for document AI, I think these type of models are super useful. Um so, you get can, for example, provide an image of a chart and then ask it certain questions. So, uh it's pretty nice that we can fine-tune them now for these type of use cases. Uh next, and this is actually something I typically do in all of the notebooks I create is just creating a regular PyTorch data set. And then, along with that, so-called PyTorch data loader. So, yeah, you can find the documentation here in the documentation of PyTorch. But, basically, this is the typical way of how you would create or yeah, a data set for fine-tuning PyTorch model on. So, basically, what you need to do is you need to inherit uh from this class over here. And then, yeah, I just define it to be custom data set. Could be any name, of course. And you need to implement three methods for for such a data set. Uh an initializing method that can be anything you want. In this case, I just I decided to provide the name of the data set, the split, and then whether or not to sort the JSON keys inside the ground truth JSONs. Yeah, this is just an additional method that I defined, JSON to token, which you are going to use to convert the ground truth JSONs into token sequences, which the model can learn to generate. Um But so, besides the init method, you also need to implement the length method, which returns the length of your data set, and the get item method, which returns one single item, one single example of your data set. Um so, as you can see, it's it's not that much code. Um I have actually used or reused um some code here from my Donut notebook. So, Donut is another document AI model. Uh there are quite a lot of notebooks here, and so I also created a notebook here for fine-tuning Donut on that same data set. So, it's actually inspired by that. Um and you will find the same kind of JSON to token method in there. Uh however, this notebook is a bit more advanced because we are also adding special tokens to the model before training, like the common JSON keys uh and so on. But that's something I'm going to skip in this notebook. Uh for simplicity, we're not going to add any special tokens to the models for training, but often times, this may also help in improving the performance even more. Um so, yeah, this init method, what we are going to do here is simply call the load data set methods, which we already called above, but Hugging Face uses caching, which means that if your data set is already available in your RAM, uh you don't need to load it uh anymore. We store the data set length here in a variable name, which you then return here. Um and then here, basically, we loop over all the samples in our data set. We load the ground truth JSONs. Um actually, yeah, this could be simplified a bit because some data sets might have like multiple ground truth parsings. That's because sometimes these data sets are annotated by multiple humans, which means that we will have we will have multiple ground truths parsings per image. But in this case, actually, we only have a single And then, we call the JSON to token method on each of these ground truth JSONs. Um so this is actually code I took from the Donut authors, but basically it converts uh yeah, the JSON sequences into token sequences which the model can learn to generate. Um so this is straightforward, returning the length of the data set, and the get item um simply takes based on an index of your data set, it takes that sample, and then it uh splits it into the image and the ground truth uh token sequence. All right, so we can actually now instantiate both the training data sets and the validation sets by simply, yeah, instantiating the class that we defined above and providing the separate splits. This is actually the cell that takes some time. Um this is probably because it needs to run this JSON-to-token uh methods on each of the ground truth JSONs. So this takes some time. We could actually add um using the tqdm uh library, we could add a progress bar here so that we can see how fast this is progressing. And then after we create the PyTorch data sets, um it's time to create the PyTorch data loaders. So every time in PyTorch you have these two types. So we have the data set first that defines the individual items or the individual examples of your data set. And next you need to define the so-called data loaders, which are going to actually provide batches of data based on the data set. And so why do we need batches? Well, that's actually because um neural networks like Gemma, PolyGemma, and so on, they're yeah, not trained on like every image and ground truth JSON one by one. We typically feed batches of data through the model because these models are optimized using uh optimizers like Adam and Adon AdamW, which is a popular optimizer for neural networks. Um and so, yeah, we need to feed batches of data through them. Um and so, that's what these data loaders will do. So, a data loader in PyTorch uh is basically going to take samples, items of your PyTorch data sets, and it's going to batch them together. And the way they are batched together, you can define that yourself by implementing a so-called collate function. So, collate functions just define how you want to batch together examples, individual items of your data set. Um and it's for that that we're going to use this processor class. Um so, in Hugging Face Transformers, you will typically in the documentation of these multi-model models, we already support quite a bit of them, as you can see right here. Um you'll typically find not only the model, so in this case, the PolyGemma for conditional generation, which is the model we're going to be fine-tuning, but also the preprocessing class, which in this case is called PolyGemma processor, which can be seen as a wrapper on top of an image processor and a tokenizer because yeah, this is a multi-model model, so we need to basically prepare both the images using the image processor and the text using the tokenizer for the model. And so, the processor class is actually going to be a wrapper on top of those. Uh and it's going to allow us to yeah, prepare the data for the model. Um so, yeah, here we're going to actually use this processor class, as you can see, and I'm defining a separate collate function for training versus evaluation. Uh the reason for that is because during training, yeah, we need to uh also provide the labels to the model besides the inputs. So, the inputs to the model will consist of the image prepared in the format of the model. So, in Hugging Face Transformers, if we prepare an image for a model, we typically then call them pixel values. So, this is typically a resized and normalized version and also rescaled version of the original image. Um and then the text inputs uh are these three here, uh the input IDs, the token type IDs, and the attention mask. Uh if you don't know what these mean, uh I would recommend to check out uh the guide on what are what are input IDs. I think we have documentation on that in the glossary. Yeah. Uh this is actually also nicely explained in this video. Um but it's basically the way we represent text for a Transformer-based model along with the token type IDs and the attention model mask. So, these are the text inputs. This is the image input prepared for the model. And then the labels, um yeah, this is actually the targets that we want the model to learn to generate. Um so, basically here what I'm doing is we take um a list of individual items that we get from our PyTorch data set. Uh so, as we saw above, the get item method returns a tuple, which also means that here we're going to get a list of tuples of the individual items. So, then we create this images list over here by taking the first item of each of these tuples, and then the labels are the second item of each of these tuples. And for the text input, yeah, I created a custom text prompt over here, which I just uh called extract JSON. So, basically, uh this model is going to get um the received image along with the text prompt extract JSON as an input. And then it's it's trained to produce the JSON as an output. So then we feed all of that to the processor class. We can feed the labels using this suffix keyword argument. We specify that we want PyTorch tensors to be returned because the model which we going to be training is all in PyTorch. So that's why we need to specify here the PT flag. And of my notebook, I use a maximum length which is the amount of tokens that we want the model to generate. And in order to batch these different JSONs together, we need to make sure that they all are all of the same exact length. That they all need to have the same amount of tokens. So that's why we do this padding and truncation over here. So padding means adding zeros basically. Or the special padding token to the text sequences so that they are all of the same length for those that are not long enough. And then for text which are too long, we are going to truncate them. Which is why we feed the truncation argument over here. But there is actually a nice guide regarding padding and truncation in the documentation as well because there are yeah, several ways to do padding and truncation. Like in this case, we only want to truncate the second sequence. Which means the text prompt. We we're not going to truncate the first sequence. And then yeah, this is a special flag. To be honest, I don't know whether this flag is now required anymore. I just copied this from the other notebook that you can find in the documentation of PolyGeMa. And then it's going to create all these inputs. So this is actually going to return a dictionary. The reason I'm simply taking each of the keys here from that dictionary and returning them as a tuple is because PyTorch Lightning had some difficulties with processing dictionaries for some reason. So, that's why I, uh, convert our dictionary here to a tuple. And then for for evaluation, uh, it looks a bit different because then we actually don't need to, uh, prepare labels for the model. So, as you can see right here, I only feed the text prompt and the uh, received images to the processor. So, it's not going to create the labels. Uh, but I do provide the ground truth answers, so the ground truth JSON sequences. Um, as an additional output here of the collate function, uh, because, uh, this is going to be useful for us, uh, in order to compare the predicted JSON sequences of the model to the ground truth, uh, answers over here because this will allow us to compute metrics, as we will see further. Um, and then creating these PyTorch data loaders, uh, for that you just need to, um, inherit from this class over here. Uh, so from torch.utils.data import data loader, very similar to how we created a PyTorch data set. You can then specify your data set to that data loader and it's going to, based on the batch size that you provide, so it's going to then batch together, uh, these examples. And the way they are batched together is defined by your collate function. And so, the typically during training, we also specify the shuffle equals true flag, which means that it's going to shuffle these examples over the data set in order to have like a random, uh, yeah, we will always have like two random received images along with their JSON sequences. And then you can check out the first batch, uh, by, yeah, iterating over that data loader because this is actually going to return so-called Python generator, over which you can, uh, iterate. Um, so, yeah, what I typically do is like taking the first batch of that data order and then simply seeing what we get. Because it's very important of course to make sure that the inputs that we feed to the model are correct because yeah, this is actually something I learned from a really nice blog post by Andrej Karpathy, a recipe for training neural networks. Which yeah, has a lot of tips and tricks regarding debugging your training neural networks and actually one of the most important things he says is like make sure to check your data like the data right before it gets fed to the neural network and in this case yeah, this is exactly the data that comes from our PyTorch data loaders. So yeah, the input IDs these will actually be just like as you can see integer numbers representing tokens in the vocabulary of the PolyJaMa model. And we can convert them back into text using the batch decode method. So this is actually what the model will get as an input. So as you can see it's like a long list of image special image tokens. And then we have our we have the special beginning of sequence token. Then we have our text prompt. And then we have the ground truth JSON. So actually maybe let me explain this quickly in Excalidraw. The way these models are trained. So let's say let's say this is our PolyJaMa model. Which yeah, is a transformer based model. So we are going to feed the input IDs the attention mask along with the token type IDs to the model. And so the input IDs yeah, they can look like this. So, I'm just going to So, a long sequence of these image tokens. Then, we have our beginning of sequence token. Then, we have the extract JSON prompt. And then, after that, we have our ground truth JSON sequence. I'm just going to copy over here the first few of those. All right. Um, and maybe let me make it a bit bigger. Like so. And then, yeah, the the attention mask itself is actually just a tensor containing like to which tokens the model should pay attention versus not. So, in this case, they are actually all set to one, which means that all of the tokens will be involved in the attention computation. So, they are going to be one for all those tokens. But, yeah, sometimes you want the model to ignore some tokens, which is why we then set them to zero. Um, and then, the token type IDs, let me check them out as well. So, in this case, yeah, I'm actually only taking a look at the first one of the batch of two. So, in this case, we actually, as you can see, we indicate, I think, which tokens are image tokens by zero. And then, which tokens are text tokens. This way, uh, the model can learn like separate embeddings for for those. So, this is actually what we going to feed to the model as an input. But, so, yeah, keep in mind like I'm I'm representing these here with text, but actually the thing that the model will see as an input are just these numbers, these input IDs. Uh so Yeah, so actually what the model will see as an input here is this followed by another integer representation of that token. Uh and then followed again by And then we have the beginning of sequence token, so that's then the token with ID 2. And so on. Uh and then yeah, let me just copy over these as well. And so on. So this is actually what the model will see as an input. Uh so let me maybe put these in red because these are like the actual inputs. Uh well, not these string representations, rather these these numbers over here. Um yeah, I I'm afraid that if I put something here in red, it's going to turn everything in red, so let me skip that. But um so we basically feed these numbers here along with the attention mask and the token type IDs to the model. And what the model is basically going to do is it's going to compute an embedding for each of those tokens in parallel using yeah, the attention mechanism. Uh so yeah, this is actually done for all of those tokens. So we have like we will have 512 tokens because we decided to pad them and truncate them all to the same length. But so yeah, it's important to know that transformers they do things in parallel, which means that Yeah. They're going to compute an embedding uh vector for each of those uh tokens. And then let's take a look at the labels because those are like the targets of the model. So as you can see, they look as follows. So they start with a lot of -100, -100, -100 and so on. So uh why is that? Well, actually for every of these tokens um the model is going to predict like which token should come next. So basically we want the model to learn that after this token over here is total for instance, we want it to predict is total price. And then after is total price, we want it to predict 50,000 which is then the actual price and so on. So the model is is going to be trained to predict the next token. Um But so the labels over here are going to look like this. So we'll have -100, -100 and so on. Um This is because we don't want image tokens to be involved in the loss computation. So if you go to the cross entropy loss, which is the loss function used by models like PolyJema or any other large language model which is trained to predict the next token, uh you will see that the ignore index here is set to -100 by default, which means that if we set labels to -100, these are not involved in the loss computation. So the model is not penalized for predicting the next token for these However, then after that after these image tokens, as you can see right here basically after the token after the token two, uh we're going to have this as the label. And then after this, the model will be trained to predict this token and so on. Um and then basically here the model is going to predict some some token like here it's going to predict six for example, token with ID six. But as the label is minus 100, we're not going to incur any loss. Um but then actually yeah here between the predictions and the labels, uh we going to compute the cross entropy loss. Um and of course yeah there's some way here to go from this embedding here that the model computes for every single token to that actual prediction. And that actually happens using a so-called language modeling head. Uh which I actually have a different video about, but basically it comes down to the model will predict like a token ID here uh for every single token in the sequence that it thinks comes next after the other token and so forth. It does this in parallel for for each of the uh tokens. So that we can also compute the cross entropy loss in parallel. So I'm just going to type here some random numbers. Uh but this way we we can compute the cross entropy loss between each of the tokens in parallel. Uh and so this is actually how each of these transformer-based models like PolyJamma but actually Llama, ChatGPT, any kind of transformer-based model uh is actually trained in this way. Um So yeah. But yeah, another thing to add here is that these labels, when user users create them, um they're typically just a copy of the input IDs. Uh with then special tokens like image being replaced by minus 100. Um but we then also need to shift the labels one position to the left so that the model always is trying to predict the next token. And this shifting itself actually happens inside the Transformers library. So, if you go to the PolyJamma model implementation and you search for the cross entropy loss, you will see here that the labels get shifted one position to the left in order to make sure that the model predicts or is trained to predict the next token. All right. So, yeah, users typically just create labels as a copy of the input IDs but with special tokens replaced by -100. All right, but I think we've already explained quite a bit here what or how these models are trained. And so, this is actually also like why I always check the inputs before I feed the the model uh but before I feed them to the model. Like here, I also check for for instance the labels. So, they're just a copy actually of the input IDs and then the shifting itself one position to the left happens internally in the model. So, this all looks fine. Uh so, actually you can also see here what the model will get as an input and what the model is predicted is required to predict. So, let me just show them for all the input IDs here and all the labels. So, as you can see right here in the very beginning, yeah, we feed these tokens as an input and the labels for them are all uh -100 -100, so the model is not trained for those. But then after some time, as you can see, okay, so apparently token with ID 2 is also a special token. So, the the label here will also be set to -100. But then um yeah, here we actually start. So, as you can see right here, the labels are just a copy of the input IDs. But so internally the model will shift this one position to the left, which means that uh this becomes the label for this input ID and this becomes the label of this input ID and so on. Right. And then we can actually check the same for the validation data loader. Uh so there we actually only feed the prompt to the model, so we don't actually have the ground truth uh JSON's here in the input IDs. Cuz we're only going to feed this prompt uh to the model. So basically it's only going to see up till here, extract JSON, and then here we're going to take the prediction like the next token that the model predicts, and then we're going to feed that additionally to the model, and again we rerun the forward pass in a loop. Um so this is called auto regressive generation. But I do have a separate video on that if you want to check it out. All right. So once we have defined the PyTorch data sets for training and evaluation, and we have defined the corresponding PyTorch data loaders, it's time to train a model. And so for training a model in PyTorch, there are very different frameworks which you could use. One popular uh class that a lot of people use is the Hugging Face Trainer API. So it provides an API to train models specifically optimized actually for models in the Hugging Face Transformers library. Um alternatives include Hugging Face Accelerate, which is a separate library developed by Hugging Face for people that want to implement their own PyTorch training runs, um and Accelerate then actually takes care of uh making sure that the model runs on a multi-GPU environment and so on. Uh so as you can see it only requires very few lines of code to be changed. Um and then a third option I would say is PyTorch Lightning, uh, which I'm going to be using in this notebook. Um, and so in PyTorch Lightning you need to define a so-called Lightning module. Um, which basically is going to organize your PyTorch code into a few sections, uh, and it's going to take care of a lot of things for us, which we often call the boilerplate, so things that we often need to redefine for every single training run, things like device placements, like making sure that the model is put on the GPU, making sure that our data is put on the GPU. Uh, it's going to take care of the backward pass, making sure that the model is put in the right mode, training versus evaluation, and so on. Uh, it also supports things like gradient accumulation, gradient checkpointing, all these kind of things that are going to help us in training a model in PyTorch very easily. Um, so you basically need to inherit from that class, the PyTorch Lightning module. Um, and then, yeah, you can implement these various methods like how does the training step needs to look like, how does the evaluation step needs to look like, which optimizer do you want to use, and then here I'm specifying my data loaders. So, as you can see, it's quite minimal. Um, and so the important thing I add here, yeah, is this distinction here between the training step and the validation step. So, the training step, as you can see, we feed the input IDs, the attention mask, and the token type IDs, as well as the pixel so that's actually something I didn't include here, but actually besides that we also feed the pixel values, which is a PyTorch tensor tensor of shape batch size, number of channels, height, and width. We also feed that to the model besides the text inputs, and then the labels are going to be used to compute the loss. So, here we feed the labels as well, and the model is going to uh, calculate the cross entropy loss for each of the tokens in parallel and it's going to return that. Um and then PyTorch Lightning is going to call the loss.backward, which then computes uh the gradients uh of the parameters with respect to the loss. And then here we have the validation step. So, this looks a bit different. So, as you can see, for inference, we use the generate method. So, in that case, we don't just uh forward the inputs through the model, as you can see right here. We don't call the model, but rather we call the generate method, which is going to do this autoregressive generation. So, so this is actually how training looks like. Where we compute the cross entropy loss. Uh yeah, computes cross entropy loss in parallel for all tokens. Um but then yeah, inference looks different looks different, sorry. Um so, let me just copy over this figure. Cuz in that case, uh we will still feed the pixel values, the input IDs, the attention mask, and the token type IDs to the model, but it's going to yeah, only feed the prompt. We're only going to feed the prompt to the model. And then because the model has been oh trained to predict the next token, Oh, sorry. I'm messing up here with this one. So, since the model has been trained on predicting the next token, uh yeah, we're not going to compute a cross entropy loss here. The model is basically going to predict a next token for each of the tokens. So, that's actually a bit funny. Like, even even at inference time, the model is going to So, inference is also called auto regressive generation for large language models. So, we get a prediction actually out for each of the tokens, but of course we're only interested in like what the model is going to predict here after this JSON token. So, then it might predict like the token with ID 85,000, let's say. Well, typically these vocabulary sizes are not as large. Let's say the token with ID 5,931. So, then we can convert that back into some sort of text, and then it might like predict this start of total token. So, this is what step one looks like in auto regressive generation. And then this process basically repeats. So, then we basically are going to feed this Oh. So, this would be step one. And then as a second step we're going to do the exact same thing. We're going to take the prediction of the model. We're going to feed that here as an additional input to the model, and then the model is again going to predict a prediction or make a prediction for every single token in the sequence in parallel. We're interested only here in what the model is predicting after that token. Might be this one, and then we can convert that back into text. So, yeah, this string representation here might be And here it might predict like an actual total price like 38,000 euros, for instance, let's say. Uh and so on. And so this is actually how autoregressive generation works. So it works in these steps. So this is step one. Uh where we feed the prompt, only the prompt to the model along with the image. Model makes a prediction for the next token, and then we take the model prediction, add it back to the prompt, and we repeat the process. And this happens until the model predicts the so-called end-of-sequence token, which is a special token in the vocabulary of the model. Uh I think you can also find this if you go to the Polyglot model, um and you check the vocabulary. There is some kind of end-of-sequence token. Uh yeah, this one. So this is a special token in the vocabulary of the model, and if the model predicts that, uh then we know that the model, yeah, wants to stop generation. And so this is the big difference, I would say, between training, where you compute the cross-entropy loss in parallel across all your tokens, whereas at inference time, we do this autoregressive generation step, where we have forward passes for each step of the generation process. And so that's what's happening what uh when you call the generate method. Um and so here I'm specifying the max new tokens argument, which is basically going to define like how many times you want to repeat this process at most. So uh we're going to do that at most 512 times. But so there are quite a bit of tricks involved in like making this autoregressive generation go as fast as possible with things like key-value caching. Uh I recommend to check that out if you want to know like how people can like speed up autoregressive generation uh with Transformer-based models, but that would require a separate video. And so then we basically get these generated token IDs back from the model, and then we can turn them back into text using the batch decode method of the processor and we're going to chop off our prompt. We don't need to prompt anymore. And then so we basically have our predicted sequences that the model has predicted and we can compare them against our ground truths uh answers that we included in our validation data loaders. And then here I'm computing the so-called Levenshtein edit distance. It's something like this. Um So what this is doing is basically computing how many edits we would need to make to the predicted sequence in order to get the ground truth sequence. And so the fewer edits we would need to make the better of course. Um and so the edit distance is like a uh formalized metric for this which is going to yeah allow us to uh track the performance over time on our validation set. So this is all also taken from the original Donut uh repository uh which is a model that I contributed to the Hugging Face Transformers library um 2 years ago and it's still a very nice end-to-end uh transformer encoder-decoder model uh for document AI. But yeah, PolyGemma is only the decoder part of Transformer. So here then I'm specifying which optimizer to use. I'm just going to be using the AdamW optimizer here which is a popular one for fine-tuning Transformers models. And here I'm specifying the data loaders. All right. Now there are actually a couple of ways to load our pre-trained model from the hub and then start training it. Um so what we used to do a lot of times was so-called full fine-tuning. So by full fine-tuning we mean we take this 3 billion parameter model and we're going to update all of the weights during our fine-tuning process. So this is known as full fine-tuning. However, yeah with these uh larger and larger models that we have nowadays, this becomes quite expensive in terms of memory usage. For instance, if you train with an optimizer like AdamW, we typically use mixed precision during training. So, what we mean by that is that we typically store the weights of the model. So, that's the precision. You can actually see that if you go here to the model, you can always see the precision on the right side. So, here in this case, it's 32 floats floating point 32, which means 32 bits or 4 bytes are used per parameter. So, that's another thing I could explain right here. The precision in which models are trained and stored. So, so if you use 32 bits, uh if you remember from high school, then 32 bits means 4 bytes because a single byte is uh 8 bits. So, for our PolyJamma model, we have 30 we have 3 billion parameters. So, if you would require 4 bytes per parameter to store them, uh that requires, yeah, 12 billion bytes or also known as 12 GB. So, that explains actually why uh if you see the files and versions that you have about 12 GB in total size for your model. Uh but so, this is also known as full precision. Uh but nowadays, we also have half precision, which is only using 16 bits or 2 bytes per parameter. So, this means for our model, 4 billion parameters times 2 bytes per parameter, so uh this would be 6 billion bytes or 6 GB, which is quite okay. Um and so during training one typically uses something called mixed precision. So mixed precision basically means that we're going to store the weights of our model in half precision, so in 16 bits. So store the model parameters in 16 bits or half precision, but store the optimizer states in 32 bits because those optimizers like AdamW, uh they typically also require quite a bit of memory during training because they also store some so-called optimizer states like second-order derivatives and things like that uh during training. And so those typically require quite high precision because otherwise, yeah, the model starts to diverge and model is not training well. So mixed precision is like um some kind of best of both worlds where we can store the parameters of the model in 16 bits, but then store the optimizer states uh in 32 bits. I think this is also explained in the documentation. Uh there's actually a very nice guide on methods and tools for efficient training on a single GPU, which explains this uh mixed precision technique. Um so yeah, this is all explained right here. But so um yeah, thing I also learned is that if you train a model with an optimizer like AdamW in mixed precision, then the amount of memory required during training is about uh the number of parameters times 18. If you train a model with AdamW during training with mixed precision, uh you need to multiply the number of parameters times 18. So in our case, we have 3 billion parameters, and if you uh multiply that by 18. uh Yeah, that's 54 GB of GPU RAM required. So, that's like Yeah, the trick I learned and that is actually also explained I think in this guide. So, it would mean that we would require 54 GB of GPU RAM to do full fine-tuning using mixed precision. Which is of course quite a bit because we are training here on an L4 Nvidia GPU. So, Nvidia L4 GPU memory is 24 GB. So, that would not fit into our GPU. So, we cannot do full fine-tuning of PolyJaMa. However, luckily there are some newer methods being developed and which are now nicely supported by Hugging Face libraries called LoRA and QLoRA. Which are basically going to allow us to train these giant models even on consumer hardware like an Nvidia GPU with only 16 or 24 GB of RAM. Yeah, LoRA itself is a method developed by Microsoft. It stands for low-rank adaptation. And the whole idea of LoRA is basically to freeze the weights of our pre-trained model. So, we're not going to touch or change anything to the weights of our pre-trained 3 billion PolyJaMa model. We're only going to add a few adapters on top of those, a few like linear layers. We're going to only train those. And this technique is known as LoRA and basically allows people to train models in a much more parameter efficient way, which is what PEFT stands for, parameter efficient fine-tuning. So, I think here there are also definitely some nice guides on that. But the main idea is basically that we're not going to touch the pre-trained weights. We're going to freeze them, keep them untouched. We're going to only add adapters on top of those. So-called adapters here shown in orange, and we're going to only train those. So, this is another way of nicely showing it. So, here we're going to freeze or not train the pre-trained weights, but only add those adapters on top, which is going to greatly reduce the amount of memory required. Um and so, this is offered in a separate PEFT library by Hugging Face. Um and then another thing which is going to even further decrease the amount of memory required for training this model is the QLoRA method, which is the idea of combining LoRA with quantization. Uh so, by quantization, well, actually um this is already a form of quantization because here we go from 32 bits to 16 bits, but you can could go even lower. Uh so, there's also int8 or nowadays, so this means 8 bits or just a single byte to store every single parameter. So, if we have 3 billion parameters um times uh just a single byte per parameter, that would mean that we only require 3 GB to load the model. And nowadays, you you even have int4 or even 2 bits or 1 bit. But let's uh like in my notebook, I'm going to be using 4 bits. So, only half a byte, which means that we only will require 1.5 GB uh to load the PolyGemma model into our notebook. Um and so, QLoRA, which is greatly explained in this uh blog post over here, is just the idea of combining LoRA with quantization. So, basically, uh it would mean that if this is our if this is our uh pre-trained PolyGemma model. Uh yeah, we're going to freeze this. So, I'm going to put it in blue. We're going to not touch on that. We're just going to add some adapters on top. Okay, I want to Yeah. Let me just call them adapters. So, that's what we're going to train and blue is what's going to stay fixed. And so, we're going to quantize this. Quantize in four bits, which means that we're only going to require 1.5 GB for loading this pre-trained model and then freezing it. And those adapters are then typically well trained in float 16, so in half precision or 16 bits. But, as we only have a few adapters, this is going to be no problem for us. So, this whole idea is known as QLoRA. And this is actually what I'm going to be using here. So, if you would do full fine-tuning, this is how you would load the pre-trained model from the Hugging Face Hub. So, you just call the from pre-trained method on the class. And yeah, typically we don't actually need to fine-tune all of the models Sorry, all of the parameters. We typically freeze In this case, the vision encoder as well as this projection layer, and we're only going to update the large language model. Of course, you could also remove this and just do full fine-tuning. It's a bit of up to experimentation what gives the best results. But, in our case, we're going to be using QLoRA. So, this is why we need to define this bits and bytes configuration. So, this basically is going to define how we're going to then or shrink down the size of the model to only four bits with yeah, some clever algorithms. So in uh the hugging face transformers library, there are a lot of quantization methods already available like AWQ, GPTQ, and bits and bytes, which is the one we will be using. So these are all like clever algorithms which come up with clever ways of shrinking down the size uh of of our parameters to fewer amount of bits while keeping the performance mostly the same. Um so this basically will define how we quantize, so how we going to quantize this into four bits. And then here the LoRA configuration then defines where we want to add these adapters on top. So that is defined using the LoRA config. Uh this is a hyperparameter regarding LoRA called the rank. Uh I just used a good default value here and then the target modules is basically defining where we need to add these adapters on top. And so we typically add them on top of the query projection, the output projection, the key projection, and value projection layers of the attention layers of the model. Uh but this is actually pretty low level because then you would like need to go to the actual implementation of a model like Gemma or PolyGemma and then you can find like where certain layers are defined. And so it's actually there that we going to add these uh adapters on top of those. And this is known to like produce uh good results. And then you need to wrap your model in this get best model. Okay, I need to first to run this cell. Um There might be an issue here with my hugging face token. Uh it says it is gated, but I have access. So, let me just restart the run time and run before. Um but uh this way yeah, we basically will see that we are only going to require 11 million parameters to be trained. So, those are the adapters here. So, only 11 million trainable parameters. And then the total amount of parameters is actually about 3 billion. So, we will have here roughly 3 billion untrainable, also called freezed parameters. Uh sorry, freezed is horrible English. I should say frozen. Sorry. Sorry for that. Um okay. So, once we have defined all that, uh it's basically just a matter of um instantiating the PyTorch Lightning module. Um and then yeah, here I'm actually uh storing some hyper parameters regarding training in a Python dictionary, such as the number of epochs, um whether we want to um yeah, check the validation metrics every so many epochs, uh whether we want to use gradient accumulation, which is another trick to uh yeah, train models on consumer hardware, the batch size we will we will be using because those are then referred to in the Lightning module. So, here I'm instantiating the Lightning module with our configuration, and then I provide things like the learning rates, the batch size, and so on to it. So, in this way we can store all these hyper parameters um in this uh dictionary. And then another thing I do is defining the callbacks, so-called callbacks. So, these are like arbitrary Python programs that we can run during our training. Uh so, here I'm will be using two callbacks, which uh one is provided by PyTorch Lightning by default. It's called early stopping. So, early stopping means that we're going to stop the training once the performance metric, so the evaluation metric on our validation step Sorry, or on on our validation set um stops improving after certain amount of epochs. So, in this case, we say that we want to stop training if the validation added distance, this Levenshtein added distance, stops improving after three uh epochs. And we need to specify that we want to minimize this metric. Um And then here I also define a so-called push to hub callback, which is going to push our model to the hub during training, so after every epoch, as well as at the end of training. This is, of course, something you can customize. Um But so, yeah, you could also of course also push uh your model as a private one, so it's such that uh not everyone in the world can can see your model. All right, so then it's time to train our model. So, um here, yeah, actually I'm providing some hyper parameters regarding training, like the max number of epochs and so on, to the trainer. Uh it's also here that you can pass the weights and biases logger um in order to track progress of your training. However, I noticed there was an issue in Google Colab uh when providing the weights and biases logger to the PyTorch Lightning trainer. Um so, we won't be doing that. Um but yeah, PyTorch Lightning takes care of automatically making sure that your model uh training runs on one one or multiple GPUs or other types of hardware. Uh and then here we provide things like gradient clipping, uh gradient accumulation, and other uh tech uh techniques that can be used to optimize the training run. So, here actually we uh are specifying that we're training with mixed precision. So, this is actually the thing I explained earlier um in this part over here. So, we're going to train these adapters uh using mixed precision. So, the optimizer states will still be in float 32 and 32-bits. Um but the adapters themselves will be loaded in 16-bits. Um and then you can see some logs over here. Uh so, we see that we only have about 11 million trainable parameters. Um and then yeah, the training is actually uh progressing. So, as you can see uh it estimates that the training itself for a single epoch takes about 4 minutes. Um we are using currently a batch size of two. This is also of course because yeah, I limited this because we're running this in Google Colab. Of course, feel free to change uh the batch size if your hardware allows it to do so. Uh and so, also feel free to play around with models with a higher image resolution and uh yeah, setting the maximum sequence length for the text tokens can also be tricked depending on your use case. Uh in our case, we're using 512 tokens. Okay, but yeah, this training um I'm just going to let it run actually until we see the first evaluation. Uh because this will allow us to see the logs, which I can actually already show maybe in the meantime by going to my Transformers tutorials tutorials repository, where you can also find my Poly Gemma notebook. Um and there I can show you how the look the logs look like during the fine-tuning run. Uh so, they look like this. Uh so, as you can see, you get like the predicted sequences together with the ground truth sequences, and then the normalized edit distance is computed, and so you can then track uh the performance over time. Uh, of course, you can also turn off this flag if you don't want to print these during training. Um, this can be set using the verbose flag, I think. Uh, yes, there's this verbose argument that I added to the configuration, and then depending on uh whether it's set to true or false, um this gets printed. All right. Uh, so we're halfway there. So, I'm just going to speed up the video a bit until we are at the end of training. All right, so we have arrived at the validation step, the first validation step. So of course you can actually tweak this um using this check val every n epoch argument, which is currently set to one, which means that after every epoch we are going to run evaluation on our validation sets. And so then you will see these logs being printed. So now we are at an edit distance of about 0.3 for this first uh example. Uh and so of course the goal will be to go uh to zero, to an edit distance of uh zero. All right, so that's for training. And so also after after this validation step is being run, the model is going going to be pushed for the first time to the hub. So this is because of the push to hub callback that we defined earlier, which will be running after the training epoch as well as the validation step. So as you can see, there are still some reading mistakes. Here it's mistakenly predicting a total price of 100,000, whereas it should be 20,000. This could have to do, of course, with the fact that it has only been trained for a single epoch right now. So there's still a lot of work there. But it looks like we are on the right track. Okay, so now as you can see, it's pushing the adapters to the hub. So now if you go to the Hugging Face Hub, you will see that it has created this repository, and in the files and versions step you can see that only these adapters are being pushed to the Hugging Face Hub. So this is a file of only about 45 megabytes. So, this is of course very small compared to the large model which is about uh 3 gigabytes in size. And then, yeah, the second epoch is going to be run, but I'm going to be pausing the training here for uh just showing you also how inference can be performed. So, at inference time, yeah, we are going to be using this generate method which implements uh the auto-regressive generation. So, in every uh forward pass, we predict one token which we append to the input, and then we predict the next token, and so on. So, for that, I'm going to be using the test set. Uh so, this is seen during training. It's also not used uh during validation because that was the validation set. So, that's actually the difference between a validation set and a test set. The test set is like not seen at all uh during training, whereas validation set can be used for things like early stopping, which means stopping the training uh when performance on the validation step uh sorry, on the validation set uh stops improving. And then, yeah, there's this PEFT integration in the Transformers library. So, if we provide our fine-tuned model ID right here, like this one, it's going to automatically load the adapters along with the so-called base model. So, the base model uh is actually the blue thing over here. Uh so, it's going to load both at the same time. Uh the reason it can do this is because if you check here the configuration of our adapters, there is a base model name or path. So, here, we know that uh this adapter has been uh pre-trained on top of the Poly Gemma model from Google. Um and so, then, yeah, we provide the inputs uh to the model. So, in this case, as you can see, it doesn't seem like there are any token type IDs being created. At inference time, we only feed the text prompt along with the image to the model. Um and then, yeah, using the generate method, we get some generated token IDs back. Um and then here I'm just uh removing the prompt from those generated token IDs, and we can convert them back into text using the batch decode method. Um so, yeah, currently it's actually autoregressively generating text. Actually, I noticed right now that I'm not leveraging the GPU. So, this was done on a CPU because actually you should add here model.to cuda, for instance, if you want to run the inference uh on a GPU. But, as you can see, it was also quite fine on the CPU. So, this is also because of the fact that the model is not too big, so it can probably also be run uh on a CPU. And then, uh here I use the token to JSON method, which you can also find in my transformers tutorials repo. Uh I took this from the Donut repository. So, Donut uh is this other document YAML, and there I defined also this token to JSON method, um which is pretty handy if you want to convert um the token sequence back into um an actual JSON object. So, if you do that right here, uh then we actually get our predicted JSON out. And of course, this is only going to improve if we train the model for more epochs, because I actually trained it for just a few. Um so, notice also that I haven't done any hyperparameter optimization yet, like number of epochs, learning rates, uh the rank of the adapters when using LoRA, uh gradient accumulation, and so on. Um I would say, yeah, play around with these because um there hasn't been any hyperparameter search being done or optimization being done yet. Uh I mainly create these these notebooks for for tutorial purposes. So this is just a disclaimer that I wanted to add. All right. So yeah, that actually concludes my video on PolyGemma fine-tuning. I hope you learned something about fine-tuning open-source transformer based models such as PolyGemma. Of course, every week nowadays there is a new model coming out. So be sure to also check out other models available in the Hugging Face Transformers library. PolyGemma is only one of the many that we support. And I suppose it's also looking at some of the current trending models like currently the Phi-3 vision model by Microsoft is also one of the newer trending small vision language models. So yeah, keep that in mind. All right. So that was it. I hope you enjoyed my video and I'll see you in the next one. Bye-bye.
