huggingface / nanoVLM Public
Code Issues 36 Pull requests 21 Discussions Actions Projects
Implementing KV Cache for inference #69
Merged andimarafioti merged 25 commits into main from kv-cache on May 27, 2025
Conversation 51 Commits 25 Checks 0 Files changed 8
Reviewers
kashif
andimarafioti
+2 more reviewers
gante
lusxvr
Assignees
No one assigned
Labels
None yet
Projects
None yet
Milestone
No milestone
Development
Successfully merging this pull request may close these issues.
None yet
7 participants
ariG23498 commented on May 22, 2025 •
edited Collaborator
Running benchmark.py (for 1000 tokens) with KV Cache:
Using device: cuda
<torch.utils.benchmark.utils.common.Measurement object at
0x750056f6b4d0>
generate_tokens(tokens, image)
setup: from __main__ import generate_tokens
15.23 s
1 measurement, 10 runs , 16 threads
Running benchmark without KV Cache:
Using device: cuda
<torch.utils.benchmark.utils.common.Measurement object at
0x7c02b496bec0>
generate_tokens(tokens, image)
setup: from __main__ import generate_tokens
15.37 s
1 measurement, 10 runs , 16 threads
Speedup: 0.9% (which is not that much). I wonder if I am missing something very big.
Update:
The speedup is about 50% now.
Here are the changes:
1. Sending one query at a time (in an autoregressive manner)
2. Carefully deciding the is_causal parameter for sdpa
2
ariG23498 added 5 commits last year
resolve merge conflicts 39289e3
merging benchmark a23a303
merging benchmark a23a303
resolving merge conflicts 259f146
Merge branch 'main' into kv-cache f5a278b…
chore: implementing KV Cache 11a9110
ariG23498 requested a review from lusxvr last year
adding cache to generate 20f3a25
ariG23498 commented on May 22, 2025 View reviewed changes
benchmark.py Outdated Show resolved
Contributor
lusxvr commented on May 22, 2025
For how many tokens did you run the generate? If it was just a few we might not see the
difference, try with a pretty big number, if the time still stays the same there is some
issue
Collaborator Author
ariG23498 commented on May 22, 2025
@lusxvr I tried it with 1000 tokens.
lusxvr mentioned this pull request on May 22, 2025
position ids for kv-cache #71 Merged
position ids for kv-cache (#71) 08e512a… Verified
Collaborator Author
ariG23498 commented on May 23, 2025
With the current implementation from @kashif we get 50% speedups!
Using device: cuda
<torch.utils.benchmark.utils.common.Measurement object at
0x7138c8315bb0>
generate_tokens(tokens, image)
setup: from __main__ import generate_tokens
9.44 s
1 measurement, 10 runs , 16 threads
2
ariG23498 marked this pull request as ready for review last year
ariG23498 requested a review from andimarafioti last year
ariG23498 changed the title [WIP] Implementing KV Cache for inference
Implementing KV Cache for inference on May 23, 2025
ariG23498 mentioned this pull request on May 23, 2025
Implement KV Caching in Language Model for Faster
Closed
Generation #27
3 tasks
kashif reviewed on May 23, 2025 View reviewed changes
models/vision_language_model.py Outdated Show resolved
kashif reviewed on May 23, 2025 View reviewed changes
models/vision_language_model.py Outdated Show resolved
Collaborator Author
ariG23498 commented on May 23, 2025
A note from @kashif
We should aslo handle training logic as the instead of one.
After the initial review, I will quickly work on this.
decoder.forward now outputs two things
1
ariG23498 and others added 2 commits last year
Update models/vision_language_model.py dc61269… Verified
Update models/vision_language_model.py 38e30be… Verified
gante reviewed on May 23, 2025 View reviewed changes
gante left a comment •
edited
In general looks good, but I think we need to benchmark memory as well: I suspect
there is some memory usage degradation (see comments below).
In general, using KV cache should have a very similar memory footprint inside the
forward pass, let's double-check that! :D
2
benchmark.py Outdated
Show resolved
models/language_model.py Outdated
Show resolved
models/language_model.py Outdated
Show resolved
ariG23498 added 2 commits last year
remove duplicated benchmark and review suggestions f40bfa5
joao's review 4690229
Collaborator Author
ariG23498 commented on May 23, 2025
@gante after adding some print statements
print(f"Phase ->{torch.cuda.memory_allocated()/1024**2:0.2f} MB\n")
I get the following
Using device: cuda
Loading weights from: lusxvr/nanoVLM-222M
Input:
What is this?
Outputs:
Before generation -> 871.11 MB
Before prefill phase -> 879.48 MB
After prefill phase-> 882.13 MB
Before next token decoding-> 882.69 MB
After next token decoding-> 882.73 MB
Before next token decoding-> 882.73 MB
After next token decoding-> 882.79 MB
Before next token decoding-> 882.79 MB
After next token decoding-> 882.82 MB
Before next token decoding-> 882.82 MB
After next token decoding-> 882.88 MB
Before next token decoding-> 882.87 MB
After next token decoding-> 882.91 MB
Before next token decoding-> 882.90 MB
After next token decoding-> 882.97 MB
Before next token decoding-> 882.96 MB
After next token decoding-> 883.00 MB
Before next token decoding-> 882.99 MB
After next token decoding-> 883.05 MB
Before next token decoding-> 883.05 MB
After next token decoding-> 883.08 MB
Before next token decoding-> 883.08 MB
After next token decoding-> 883.14 MB
Before next token decoding-> 883.14 MB
After next token decoding-> 883.17 MB
Before next token decoding-> 883.17 MB
After next token decoding-> 883.23 MB
Before next token decoding-> 883.23 MB
After next token decoding-> 883.26 MB
Before next token decoding-> 883.26 MB
After next token decoding-> 883.32 MB
Before next token decoding-> 883.32 MB
After next token decoding-> 883.35 MB
Before next token decoding-> 883.35 MB
After next token decoding-> 883.41 MB
Before next token decoding-> 883.41 MB
After next token decoding-> 883.44 MB
Before next token decoding-> 883.44 MB
After next token decoding-> 883.50 MB
Before next token decoding-> 883.50 MB
After next token decoding-> 883.53 MB
Before next token decoding-> 883.53 MB
After next token decoding-> 883.59 MB
This is in alignment to what you have said. Would this be enough to conclude that
This is in alignment to what you have said. Would this be enough to conclude that
memory requirements are okay?
1
kashif added 2 commits last year
fix self.decoder() call d32f6cf
add back new line 87895e1
lusxvr commented on May 23, 2025 •
edited Contributor
We should aslo handle training logic as the decoder.forward now outputs two
things instead of one.
Indeed! We need to make sure we don't break the training pipeline when we merge this!
I can help with testing the training pipeline if you need.
But super great progress on this, very impressed @ariG23498 @kashif :)
3
kashif commented on May 23, 2025 •
edited Collaborator
@lusxvr all good with regards to the training... the kv_cache output is now ignored
2
26 hidden items
Load more…
Load more…
Contributor
lusxvr commented on May 26, 2025
are you sure you were doing greedy decoding? The default in train.py now is to not
use greedy decoding.
great catch, I did not change the default (somehow I just assumed it would be enabled)
Contributor
lusxvr commented on May 26, 2025
@kashif sounds good to me, do you have anything against me squashing the commits
when I merge?
Collaborator
kashif commented on May 26, 2025
squash away! I love squash
tests 35ec3a4
andimarafioti requested changes on May 26, 2025 View reviewed changes
andimarafioti left a comment •
edited Member
I wrote two tests and they don't pass. The first one that I think should trivially pass can
be run with:
python -m unittest tests.test_language_model
The second one is a bit more involved, I had to make small changes to the VLM model
file to make the test runnable, and I'm not sure passing 0 is the right way to do it when
not using the KV cache. Let me know if I messed up the tests, but also feel free to
not using the KV cache. Let me know if I messed up the tests, but also feel free to
modify them into something similar that passes. Ideally, the results with/without kv-
cache should be identical, right?
Second test is:
python -m unittest tests.test_vision_language_model
Collaborator Author
ariG23498 commented on May 26, 2025
Hi @andimarafioti
Thanks for the tests! I think they might have uncovered something severe. The norms
used in the language models are causing the problem and not the KV Cache in place.
If you run the following, it will fail with the current implementation. My idea here is to
test the model's outputs if you provide the entire sequence and the input sequence
with the last input token left out.
import torch
import unittest
from models.language_model import LanguageModel
from types import SimpleNamespace
class TestLanguageModel(unittest.TestCase):
def setUp(self):
# Minimal config for testing
self.cfg = SimpleNamespace(
lm_hidden_dim=64,
lm_inter_dim=128,
lm_rms_eps=1e-5,
lm_re_base=10000.0,
lm_max_position_embeddings=512,
lm_attn_scaling=1.0,
lm_vocab_size=100, # Small vocab for testing
lm_n_heads=4,
lm_n_kv_heads=2,
lm_dropout=0.0,
lm_n_blocks=2,
lm_use_tokens=True,
lm_tie_weights=True
)
self.model = LanguageModel(self.cfg)
self.model.eval() # Set model to evaluation mode
def test_kv_caching_consistency(self):
def test_kv_caching_consistency(self):
# Input for the model
batch_size = 16
seq_len = 1000
input_ids = torch.randint(0, self.cfg.lm_vocab_size, (batch_size, seq_len))
# Forward pass without KV caching (prefill)
output_no_cache1, _
= self.model(input_ids, start_pos=0)
output_no_cache2, _
= self.model(input_ids, start_pos=0)
self.assertTrue(
torch.allclose(output_no_cache1, output_no_cache2, atol=1e-5),
"Outputs do not match"
)
output_no_cache1, _
output_no_cache2, _
= self.model(input_ids, start_pos=0)
= self.model(input_ids[:, :-1], start_pos=0)
self.assertTrue(
torch.allclose(output_no_cache1[:, :-1, :], output_no_cache2, atol=1e-5),
"Outputs do not match"
)
if __name__ == '__main__':
unittest.main()
Doing a similar test on a language model from transformers pass
from transformers import AutoTokenizer, AutoModel
tok = AutoTokenizer.from_pretrained("meta-llama/Llama-3.2-1B")
model = AutoModel.from_pretrained("meta-llama/Llama-3.2-1B")
inputs = tok("Hello, my dog is ", return_tensors="pt")
out1 = model(inputs["input_ids"]).last_hidden_state
out2 = model(inputs["input_ids"][:, :-1]).last_hidden_state
torch.testing.assert_close(
out1[:, :-1, :],
out2,
)
If you comment out all the norm calls in the language model and run the tests that you
have written, the tests pass. We might need to create another issue for this and figure
out the problem (my bet is on the norms).
All the norm calls are:
1. nanoVLM/models/language_model.py
Line 246 in 1ce10cf
246 x = self.norm(x)
246 x = self.norm(x)
2. nanoVLM/models/language_model.py
Line 193 in 1ce10cf
193 x = self.norm1(x)
3. nanoVLM/models/language_model.py
Line 198 in 1ce10cf
198 x = self.norm2(x)
CC: @kashif
Member
andimarafioti commented on May 26, 2025
Hi, those norms follow quite closely the implementation in transformers'
modeling_llama.py file. I just checked. I'll see if I can do a similar experiment with a
transformers model.
1
Member
andimarafioti commented on May 26, 2025
FYI, I tried the same with transformers and the versions with and without cache are
identical:
import torch
from transformers import AutoTokenizer, AutoModelForCausalLM
import warnings
# Suppress a specific UserWarning from transformers
warnings.filterwarnings("ignore", message="The QuietFreezeWrapper class will be depre
warnings.filterwarnings("ignore", message="Passing the following arguments to `inputs
def test_kv_caching_consistency_hf(model_name="meta-llama/Llama-2-7b-chat-hf", device
"""
Tests KV caching consistency for a Hugging Face transformer model.
1. Compares last token logits: (full pass) vs (prefill + 1 decode step).
2. Compares full sequence logits: (full pass) vs (token-by-token decode).
"""
print(f"Loading tokenizer and model: {model_name}...")
# It's good practice to use trust_remote_code=True for some models,
# though for standard Llama it might not be strictly necessary.
# Adding it preemptively.
# Adding it preemptively.
tokenizer = AutoTokenizer.from_pretrained(model_name, trust_remote_code=True)
model = AutoModelForCausalLM.from_pretrained(
model_name,
torch_dtype=torch.bfloat16,
trust_remote_code=True
).to(device)
model.eval()
# Test parameters
batch_size = 16 # Keep small to avoid OOM, can be increased
seq_len = 320 # Keep small for faster testing, can be increased
vocab_size = tokenizer.vocab_size
print(f"Generating random input_ids (batch_size={batch_size}, seq_len={seq_len},
input_ids = torch.randint(0, vocab_size, (batch_size, seq_len), device=model.devi
print("\n--- Test 1: Last Token Consistency ---")
# Forward pass without explicit KV caching reuse (but cache is computed)
print("Running full forward pass (reference)...")
with torch.no_grad():
outputs_no_cache = model(input_ids, use_cache=True) # use_cache=True to get p
logits_no_cache = outputs_no_cache.logits
# Forward pass with KV caching
# 1. Prefill phase
print("Running prefill phase (all but last token)...")
prefill_input_ids = input_ids[:, :-1]
with torch.no_grad():
outputs_prefill = model(prefill_input_ids, use_cache=True)
kv_cache_prefill = outputs_prefill.past_key_values
# 2. Decode phase (last token)
print("Running decode phase (last token using KV cache)...")
last_token_input_ids = input_ids[:, -1].unsqueeze(-1) # Shape: [B, 1]
with torch.no_grad():
outputs_with_cache_last_token = model(
last_token_input_ids,
past_key_values=kv_cache_prefill,
use_cache=True
)
logits_with_cache_last_token = outputs_with_cache_last_token.logits
# Compare the logits for the last token
logits_no_cache_last_token = logits_no_cache[:, -1, :]
# logits_with_cache_last_token is [B, 1, vocab_size], so squeeze or select
logits_with_cache_last_token_squeezed = logits_with_cache_last_token[:, 0, :]
atol = 1e-6
last_token_match = torch.allclose(logits_no_cache_last_token, logits_with_cache_l
print(f"Logits for the last token match: {last_token_match}")
if not last_token_match:
print("Max difference (last token):", (logits_no_cache_last_token - logits_wi
print("\n--- Test 2: Full Sequence Consistency (Token-by-Token) ---")
# Reset for a full token-by-token generation using KV cache
print("Running token-by-token generation with KV cache...")
output_logits_list_step_by_step = []
current_kv_cache = None
for i in range(seq_len):
current_input_id_step = input_ids[:, i:i+1] # Current token
with torch.no_grad():
outputs_step = model(
current_input_id_step,
past_key_values=current_kv_cache,
use_cache=True
)
# outputs_step.logits is [B, 1, vocab_size]
output_logits_list_step_by_step.append(outputs_step.logits)
current_kv_cache = outputs_step.past_key_values
if i % 5 == 0 or i == seq_len -1 :
print(f" Processed token {i+1}/{seq_len}")
# Concatenate all single token logits
logits_with_cache_full_sequence = torch.cat(output_logits_list_step_by_step, dim=
full_sequence_match = torch.allclose(logits_no_cache, logits_with_cache_full_sequ
print(f"Full sequence logits match (token-by-token vs full pass): {full_sequence_
if not full_sequence_match:
print("Max difference (full sequence):", (logits_no_cache - logits_with_cache
print("\nTest completed.")
if __name__ == '__main__':
model_to_test = "HuggingFaceTB/SmolLM2-360M-Instruct"
selected_device = "cuda:0" if torch.cuda.is_available() else "cpu"
print(f"Using device: {selected_device}")
test_kv_caching_consistency_hf(model_name=model_to_test, device=selected_device)
Member
andimarafioti commented on May 26, 2025
Looking further, the norms in transformers do convert the values to float32 to process. I
applied that change and it didn't change the results. I agree that your test @ariG23498
should also work. The reason why it does without norms is that the values are in a
different range and then the absolute tolerance you accept is sufficient.
andimarafioti commented on May 26, 2025 Member
on May 26, 2025
Ok, I checked Aritra's test on main and it also fails !
fix language model test 88a73ba
andimarafioti commented on May 26, 2025 •
edited Member
Figured out why Aritra's test was failing. If seq_len > lm_max_position_embeddings ,
then the RoPE scale is computed on the seq_len. Because they changed between the
two examples, the values are different. Setting lm_
max
_position_embeddings to 1024
fixes it.
1
andimarafioti commented on May 26, 2025 •
edited Member
Some more info:
Loading backbones the test still fails, even more than with the small test
Setting both times to use or not use the cache gives us deterministic results, so the
difference is between having the cache or not.
passing start_pos=current_token_start_pos instead of zero when not using
kv
cache still fails
_
kashif's fix to make kv cache true/false produce same results b57fca1
Member
andimarafioti commented on May 26, 2025
I can't figure out why, but I'm also getting the same issue that @lusxvr had. Basically,
training on main or on this branch gives very different accuracies
Member
andimarafioti commented on May 26, 2025
I created a script to test MMStar directly on a pretrained model. And evaluating it on
main or on this branch gives different results. I changed the test_mmstar to use greedy
decoding for my tests for reproducibility. On this branch, with or without kv-cache it
gives me the same (worse) results. Here is my script:
import argparse
import torch
import os
import numpy
import random
from datasets import load_dataset
from torch.utils.data import DataLoader
# Imports from the project
from models.vision_language_model import VisionLanguageModel
from data.processors import get_tokenizer, get_image_processor
from data.datasets import MMStarDataset
from data.collators import MMStarCollator
from train import test_mmstar # Using the function from train.py
# models.utils is implicitly used by test_mmstar
# Set TOKENIZERS_PARALLELISM to avoid warnings
os.environ["TOKENIZERS_PARALLELISM"] = "false"
# Seed for reproducibility
torch.manual_seed(0)
if torch.cuda.is_available():
torch.cuda.manual_seed_all(0)
numpy.random.seed(0)
random.seed(0)
MMS
random.seed(0)
def seed_worker(worker_id):
worker_seed = torch.initial_seed() % 2**32
numpy.random.seed(worker_seed)
random.seed(worker_seed)
def parse_args():
parser = argparse.ArgumentParser(description="Evaluate a nanoVLM model on the parser.add_argument(
"--checkpoint", type=str, default=None,
help="Path to a local checkpoint (directory or safetensors/pth). If omitted,
)
parser.add_argument(
"--hf_model", type=str, default="lusxvr/nanoVLM-222M",
help="HuggingFace repo ID to download from if --checkpoint isn't set."
)
parser.add_argument(
"--dataset_path", type=str, default="Lin-Chen/MMStar", # Matches typical trai
help="Path or HuggingFace dataset ID for the MMStar test dataset. Script will
)
parser.add_argument(
"--mmstar_batch_size", type=int, default=256,
help="Batch size for MMStar evaluation."
)
parser.add_argument(
"--num_workers", type=int, default=4,
help="Number of workers for the DataLoader."
)
return parser.parse_args()
def main():
args = parse_args()
# Setup device
if torch.cuda.is_available():
device = torch.device("cuda")
elif hasattr(torch.backends, "mps") and torch.backends.mps.is_available():
device = torch.device("mps")
if device.type == "mps": # Ensure mps fallback if needed
torch.backends.mps.enable_fallback_to_cpu = True
else:
device = torch.device("cpu")
print(f"Using device: {device}")
# Load model
source = args.checkpoint if args.checkpoint else args.hf_model
print(f"Loading model weights from: {source}")
model = VisionLanguageModel.from_pretrained(source).to(device)
model.eval() # Set model to evaluation mode
# Get tokenizer and image processor from model's config
tokenizer = get_tokenizer(model.cfg.lm_tokenizer)
image_processor = get_image_processor(model.cfg.vit_img_size)
image_processor = get_image_processor(model.cfg.vit_img_size)
# Load test dataset (using 'val' split, similar to train.py for MMStar)
print(f"Loading dataset: {args.dataset_path}, attempting to use 'val' split.")
try:
dataset_dict = load_dataset(args.dataset_path)
if 'val' not in dataset_dict:
raise KeyError(f"The 'val' split was not found in dataset {args.dataset_p
test_ds_split = dataset_dict['val']
except Exception as e:
print(f"Error loading dataset or finding 'val' split: {e}")
return
test_dataset = MMStarDataset(test_ds_split, tokenizer, image_processor)
# Create MMStar collator
mmstar_collator = MMStarCollator(tokenizer)
# Create DataLoader
g = torch.Generator()
g.manual_seed(0) # Seed for DataLoader
test_loader = DataLoader(
test_dataset,
batch_size=args.mmstar_batch_size,
shuffle=False, # Evaluation is typically not shuffled
collate_fn=mmstar_collator,
num_workers=args.num_workers,
pin_memory=True, # Matches train.py test_loader configuration
worker_init_fn=seed_worker if args.num_workers > 0 else None,
generator=g
)
# Run MMStar evaluation
print("Starting MMStar evaluation...")
accuracy = test_mmstar(model, tokenizer, test_loader, device)
print(f"MMStar Test Accuracy on 'val' split of {args.dataset_path}: {accuracy:.4f
if __name__ == "__main__":
main()
I run it with:
python evaluate.py --hf_model andito/nanoVLM
ariG23498 commented on May 26, 2025 • edited Collaborator Author
Thanks for the script @andimarafioti
Some more info:
1. I ran a one to one check of the inputs, and they were the same
2. Iterating on each batch, I found out that some batches have lower hits, and I am
currently looking into them
3. I also tried using use_kv_cache=False with the kv-cache branch, and it resulted
in an OOM (which signifies that there is more memory being used). This intuitively
makes sense because with use_kv_cache=False we are still creating the cache,
but also not using it. This needs to be looked into
4. Using sdpa=False in main branch works, while sdpa=False with kv-cache
branch leads to OOM
ddiddi commented on May 27, 2025
LGTM! Appreciate the efforts on this one.
kashif added 5 commits last year
fix is_causal condition 40c30cd
add back the attention_mask in the generate 8c87101
attention_mask=attention_mask, 1e1e95c…
set greedy=True 24e025d
greedy=True 862bc77
andimarafioti approved these changes on May 27, 2025 View reviewed changes
Member
andimarafioti left a comment
Thank you so much for going through this! I'm sorry for the thousand reviews, but I'm
super happy with the results!
1
andimarafioti merged commit a173568 into main
on May 27, 2025
andimarafioti deleted the kv-cache
branch last year
Contributor
lusxvr commented on May 27, 2025
Crazy effort by you guys, kudos for pushing through!
2
sang-nguyen-ts pushed a commit to tsdocode/nanoVLM that referenced this pull
request on May 28, 2025
Merge pull request huggingface#69 from huggingface/kv-cache 817f131
…
tsdocode mentioned this pull request on May 29, 2025
Perf: Adding torch.compile + static cache, ~3x speed up #88 Open
pgryko pushed a commit to pgryko/nanoVLM that referenced this pull request
on Jul 6, 2025
Merge pull request huggingface#69 from huggingface/kv-cache ba68ec0
…
Sign up for free
to join this conversation on GitHub. Already have an account? Sign in to
comment
