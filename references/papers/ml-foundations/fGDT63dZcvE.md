# YouTube

## Backpropagation: How Neural Networks Learn

### Video Metadata
- **Keywords:** AI,ml,ML,DL,deep learning,machine learning,optimization,backprop,Hinton,neural networks,artificial intelligence,education,computer science,learning theory,training,neuron,weight connection,connectionism,deep neural networks,don't,feed-forward,fully connected layer,multilayer perception,connectionist models,artificial neural network,technology
- **Runtime:** PT10M16S

### Description
A brief intro to the algorithm that powers virtually all neural network training today.

Timestamps
--------------------
Introduction 00:00
Neural network overview 00:31
Gradient descent 03:04
The backpropagation algorithm 05:05

### Transcript
neural networks derive their power from being a highly General model whose parameters can be optimized to fulfill a particular task however one prominent challenge was exactly how to get those parameters optimized given some Target and input data one answer that has proved highly influential is back propagation proposed by David rummahart Jeff Hinton and Ronald Williams in 1986. now before exploring that method let's touch briefly on neural network architecture a feed forward neural network generally consists of a set of n layers each one feeding inputs into the next here is a schematic of the simplest kind of neural network a multi-layer perceptron where each neuron in one layer is connected to every neuron in the next layer now when a neuron receives inputs from the previous layer it calculates a weighted sum of them plus some bias term the result is then passed through an activation function which can be any function in theory to produce the output hence the full formula is this where X hat just represents the pre-activation output now here are some commonly used activation functions the linear activation just leaves X hat untouched while the relu or rectified linear unit activation converts negative values to zero and the sigmoid activation reduces inputs to the zero to one range using this particular formula now while the architecture of the network is typically fixed during training these inter-layer weights Omega are allowed to change and it is the weight configuration that specifies the task for which the network is Suited so the process of neural network training is the process of optimizing these weights via the use of a loss function and an optimization algorithm now when an input is received by a multi-layer perceptron as a whole it produces an output Vector Y at each layer from 1 to n with a loss function that typically though not always is applied on the outputs of the final layer and this loss function is just a metric that defines the penalty given to the model during training now since the loss value depends directly only on the final layer outputs it depends on the rest of the weights of the network only indirectly and if we can calculate the gradient of the loss with respect to each of these weights in the network we would know how to shift these weight values in order to reduce loss the idea here is that even though those other weights do not affect the loss function directly they do affect it in the sense that they affect the output Vector Y at the final layer now this whole process is called gradient descent we calculate the partial derivative of the loss with respect to every individual weight in the network represented what by this weight Vector W and then we push the weights in the opposite direction of this gradient using a step that's just scaled by the Learning rate here but don't worry about that now now the overall goal is to find a global minimum of loss or barring this at least a local minimum that is not much worse than the global minimum however at times we can certainly find ourselves stuck in positions where good Minima are impossible to reach given the existence of barriers in the Lost landscape but that problem is mitigated by allowing the model to Traverse a high dimensional weight space in other words to contain a very large number of Weights in the network the authors of the original paper in fact pointed out that high dimensional weight spaces tend to provide opportunities to overcome barriers that would otherwise hobble training in lower dimensional spaces to take an example imagine that instead of just Omega 1 we now have a second weight called Omega 2. now it might be that in this two-dimensional rather than one-dimensional weight space some sort of path exists that would allow the model to travel to the global minimum without having to Traverse a barrier so this is the general scheme but the question remains how do we derive the par the partial derivative of the loss with respect to each weight in the network this method of gradient descent only works if all the weights are changed in tandem or else all the benefits of having a very large weight space are not felt so how exactly do we derive the directions in which these weights have to be changed well that brings us to back propagation suppose we're trying to calculate the gradient assigned to the weight between one neuron and the penultimate layer here on I and one neuron in the final layer neuron J the first step in this procedure is to calculate the gradient of loss with respect to the output of neuron J this would just depend on the form of the loss function so it is easily calculable then we work out the gradient with respect to the pre-activated output of neuron J this can be just done by via the chain rule of differentiation where we know DL by d y j from Step One now d y j over d x hat J just depends on the activation function being used and for now let's just use sigmoid as an example in this case differentiating with respect to X hat J is straightforward and it gives us this expression here so now the next step is to calculate DL over D Omega i j which is what we want and again this can be done using the chain rule in this expression d x hat J over D Omega i j simply just evaluates to d uh sorry just evaluates to y i since uh the contribution of Omega i j x hat J is scaled by y i and here we can also use the expression for DL over d x hat J that we derived in step two so now we have an expression for the gradient of loss with respect to the weight of Interest using values of variables that are readily available now we know that this process can be done for every neuron in the nth or final layer if we have DL over d y we can calculate DL over D Omega but what about the other layers in the network well the really great thing about back propagation is that we can also calculate DL by d y for layer n minus 1. given that we know the value for layer n and so of course from this we can then find the value of DL over D omega as well for layer n minus one and if we do this iteratively we could then find DL by D Omega for any layer m in the network and in this whole process we can see that this step highlighted here is crucial but how is it actually done well say we have a neuron I which is connected to every neuron K in the following layer then DL over DYI where DYI is just the output of neuron I equals the sum of this chain rule expression over every Target neuron K and remember that we can evaluate DX k d x hat k Sorry by DYI as Omega i k and from our previous working we can also Express DL by D X hat K using this expression here hence we can calculate DL over DYI just by using three variables one is DL over Dyk where d y k is the output of the neurons of the next layer and then d y k by D X hat K which just depends on the activation function and then Omega i k which is just the weight which is already known hence it is very feasible to calculate DL by d y for the previous layer so in general if we have a neuron p and neurons in the next layer represented by Q and we want to find the weight uh how the weight PQ should change this entire back propagation algorithm can be considered as powered by these two equations equation 1 allows us to calculate DL by d y for neuron P given the L by d y for every neuron Q in the next layer equation 2 then allows us to calculate the gradient of L with respect to the weight between neuron p and one specific neuron Q given DL by d y q so equation one is responsible for this horizontal motion here back propagating all the way to layer 1 from layer n and equation two is responsible for translating DL by d y to DL by D Omega for each neuron hence this procedure is named the backward propagation of errors or back propagation for short and this way every weight can be adjusted simultaneously such as the network as a whole goes down the loss gradient and this can be done through several optimization procedures or optimizers but that's a subject for another video for now thanks for watching
